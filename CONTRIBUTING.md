# 贡献与编码守则

本文定义新增功能、兼容适配、重构和文档修改时必须遵守的项目规则。

## 修改前

1. 阅读根目录 `README.md`、`docs/architecture.md` 和目标模块的 README。
2. 明确修改属于通用钩子、字体数据、UI、配置，还是引擎适配器。
3. 优先复用现有接口和状态，不在多个钩子中复制相同判断。
4. 保留用户已有配置的默认行为；新增行为必须可以关闭或可靠自动检测。

## 模块与文件放置

- `dllmain.cpp` 只负责安装入口和退出信号，不放业务逻辑。
- 通用 Detours 处理程序放在 `hooks/internal/font_hooks_*.cppinc`。
- 字体替换模型放在 `hooks/internal/model/`，字体查询适配放在
  `hooks/internal/queries/`。
- 引擎专用检测、缓存和资源转换放在 `hooks/internal/engines/<engine>/`。
- 原始 SFNT 字体表修改只放在 `font/`。
- UI 绘制、输入和配置应用分别放在 `ui/internal/paint`、`input` 和
  `font_picker_apply_config.cppinc`。

不要为只有一个调用点的简单转发函数创建新模块。新模块应在较小接口后封装明显的
实现复杂度，并提高修改时的局部性。

## 钩子规则

1. 保存原始函数指针后再安装 Detours。
2. 钩子内部调用真实 API 时优先使用 `org*` 指针。
3. 明确处理选择器线程和内部字体探测，避免探测过程再次触发字体替换。
4. 在进入高风险调用前保存状态，所有成功和失败路径都要恢复 HDC、字体和线程状态。
5. 不在 `DllMain` 的退出路径等待线程或执行可能获取加载器锁的操作。
6. 新钩子加入对应的 `Attach*Hooks` 分组，不在安装函数中堆叠无关逻辑。

## 字体与编码规则

- 字体名称、HFONT、字体表和文本字节流属于不同职责，禁止混合处理。
- 换字体不得自动强制 CP932、CP936 或其他代码页。
- 字符集替换只修改显式字符集接口；代码页重定向只修改转换接口；文字映射只修改
  启用映射后的字符数据。
- ANSI 文本优先使用程序原始字体创建阶段检测到的字符集；
  `TextSubstitutionCodepage` 仅作为无法检测时的回退。
- 单字形 ANSI 参数需要同时考虑字符值的字节顺序和目标代码页，不得把无效序列
  静默解释为另一个字符。
- 修改 `cmap`、`name`、`OS/2`、`hhea` 或 `head` 表后必须更新对应表校验和及
  `checkSumAdjustment`。

## 引擎适配规则

1. 先检查配置和低开销路径标记，再执行模块扫描或资源解析。
2. 检测结果在进程生命周期内缓存。
3. 通用 GDI、文件或窗口特征只能作为辅助证据，不能单独判定具体引擎。
4. 文件扩展名和通用归档名只负责候选路由；身份确认使用模块、导出、框架布局或已解析
   容器中的引擎资源。
5. 引擎不匹配时不得修改导入表、文件属性、缓存或文本数据。
6. 文件虚拟化统一经过 `engine_file_dispatch.cppinc`，A/W 钩子共享同一优先级逻辑。
7. 热切换必须使用 `Config::ConfigVersion` 标记缓存版本，并提供失败回退。
8. 每个引擎目录维护独立 README，并登记到 `hooks/internal/engines/README.md`。

## 性能与 DLL 体积

- 高频钩子避免堆分配、磁盘访问、模块遍历、归档解析和无界日志。
- 不可变探测结果、路径和导出解析结果只计算一次。
- 锁保护范围只覆盖共享状态，不在持锁期间调用引擎、Python、Mono 或 IL2CPP。
- 优先使用已有 Win32 API 和标准库，不增加非必要运行时依赖。
- Release 保持函数级链接、字符串池、COMDAT 折叠、无 RTTI 和体积优先优化。
- 引入大表、模板实例或第三方库时记录 Win32/x64 DLL 体积变化。

## 配置规则

- 配置项在 `framework.h` 声明，在 `utils.cpp` 提供默认值、保存和加载逻辑。
- 布尔配置使用 `0/1`；数值配置加载后必须进行范围校验。
- UI 可修改的配置必须同步更新保存状态、界面状态和 `NotifyConfigChanged` 路径。
- 配置关闭时应恢复未安装本 DLL 时的行为。
- 配置键重命名时提供兼容别名或明确迁移逻辑。

## 线程与生命周期

- 工作线程必须响应 `Utils::IsShuttingDown()` 或自身停止事件。
- 正常退出路径负责停止线程和关闭句柄；`DLL_PROCESS_DETACH` 只发布退出信号。
- 跨线程应用字体时使用窗口消息、事件或运行时提供的调度接口。
- 不将未固定的 Mono/IL2CPP 托管对象跨 GC 周期保存；需要长期持有时使用运行时句柄。

## 日志规则

- 默认关闭调试日志。
- 高频日志必须受采样计数限制。
- 日志至少包含模块标签、决策结果和关键版本号，不输出大块字体或资源数据。
- 兼容判断应记录“为何启用或跳过”，避免只记录最终失败。

## 文档规则

- 文档使用简体中文和 UTF-8；代码标识符、API、文件名、配置键和引擎名称不翻译。
- README 记录当前职责、功能、接口、配置、不变量、生命周期和扩展步骤。
- 引擎 README 统一包含：职责、检测条件、文件入口、功能、配置、运行约束和验证。
- 使用“负责、提供、处理、支持”等当前状态表述，代码变更历史进入提交记录或 ADR。
- 临时调试过程、单个游戏的试验参数和一次性日志不写入 README。
- 同一事实只保留一个权威位置，其他文档使用相对链接引用。
- 修改模块接口、检测条件、配置或线程生命周期时，在同一次修改中更新文档。
- 模块 README 使用以下结构：职责、入口与依赖、流程、不变量、配置、扩展步骤、验证。

## 验证清单

- 文档修改：检查相对链接、标题层级、UTF-8 编码和代码标识符。
- 通用 C++ 修改：编译 Win32 与 x64 Release。
- 代理或链接修改：比较导出名称和序号，确认目标仍为 `winmm.dll`。
- 字体表修改：覆盖 TTF、OTF、TTC、损坏输入和校验和。
- 引擎修改：至少覆盖正向检测、负向检测、功能关闭和热切换。
- 性能修改：确认高频路径没有新增重复扫描、无界分配或无界日志。
